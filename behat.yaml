default:
  suites:
    register_member:
      contexts:
        - 'App\Features\RegisterMemberContext'
      paths:
        - "%paths.base%/features/register_member.feature"
    register_admin:
      contexts:
        - 'App\Features\RegisterAdminContext'
      paths:
        - "%paths.base%/features/register_admin.feature"
    update_resume:
      contexts:
        - 'App\Features\UpdateResumeContext'
      paths:
        - "%paths.base%/features/update_resume.feature"
    create_post:
      contexts:
        - 'App\Features\CreatePostContext'
      paths:
        - "%paths.base%/features/create_post.feature"

